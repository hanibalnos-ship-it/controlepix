<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Intencionalmente NÃO existe permissão INTERNET neste app. -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_pix_control"
        android:label="Controle Pix"
        android:supportsRtl="true"
        android:theme="@style/Theme.ControlePix">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".notification.PixNotificationListenerService"
            android:exported="false"
            android:label="Leitor de Pix recebidos"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
